apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-rabbitmq.fullname" . }}-test-connection"
  labels:
    {{- include "helm-rabbitmq.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-rabbitmq.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
