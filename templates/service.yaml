apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-rabbitmq.fullname" . }}
  labels:
    {{- include "helm-rabbitmq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-rabbitmq.selectorLabels" . | nindent 4 }}
